- Run `bun run check-types` to check type.
- priortize to use `satisfies` over `as` or `!`.
- inline the query inside the `db.query` of the drizzle directly. Don't intermediate variables to store the query condition.
- always use `karpathy-guideliness` skill

